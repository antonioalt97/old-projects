select CONCAT(first_name,CONCAT(' ', last_name)) "Empleado" , NVl(TO_CHAR(manager_id),'No posee jefe') "JEFE" from employees ;

select employee_id empleado, salary SALARIO, trunc(salary/1000) "PORCENTAJE" from employees;

select first_name nombre , last_name apellido, job_id trabajo, concat(concat( initcap (SUBSTR(first_name,1,3 )),Length(first_name)),job_id)  "NOMBRE_USUARIO",
concat(concat(trim( '/' from hire_date),substr(hire_date,2,6)),substr(last_name,3) ) 

 from employees order by last_name;
 
 
 
 select concat(concat(last_name,' posee un salario de '), salary) "LISTADO DE SALARIOS" from employees;




----------------------------------

join + group by orden 

select department_name , (count(employee_id)) from employees join departments using (department_id)
group by department_name
having (count(employee_id) = (select max(count(employee_id)) from employees group by department_id))

order by department_name;
