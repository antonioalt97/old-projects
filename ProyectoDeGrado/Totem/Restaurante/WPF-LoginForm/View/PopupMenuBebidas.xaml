<UserControl x:Class="WPF_LoginForm.View.PopupMenuBebidas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_LoginForm.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox ItemsSource="{Binding MenuItems}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="0,0,0,1">
                        <StackPanel Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top">
                            <Image Source="{Binding Imagen}" Width="100" Height="100">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Opacity" Value="1" />
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.8" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock Text="{Binding IdMenu}" HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding Nombre}" HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding Precio, StringFormat=C}" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Cerrar" Click="CloseButtonClick" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10,0,0" Background="#FFE40000"/>

    </Grid>
</UserControl>
